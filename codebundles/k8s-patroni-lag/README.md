# Kubernetes Patroni Replica Lag Measure

## SLI
Uses kubectl (or equivalent) to query the lag of replicas in a Patroni cluster within a Kubernetes namespace.

## Use Cases

## Requirements

## TODO
- [ ] Add additional documentation